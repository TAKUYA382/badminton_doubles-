<%= turbo_frame_tag dom_id(match) do %>
  <!-- 未保存ハイライト対象（紫枠） -->
  <div class="match-card" data-match-id="<%= match.id %>" data-match-el>
    <div class="match-card__header">
      <span><%= match.round.index %>回戦</span>
      <span><%= match.court_number %>コート</span>
      <% if match.respond_to?(:gender_label) %>
        <span><%= match.gender_label %></span>
      <% end %>
      <!-- 「未保存」バッジ（バッファに積まれたら表示） -->
      <span class="badge-unsaved" data-unsaved-badge style="display:none;">未保存</span>
    </div>

    <div class="match-card__body">
      <table class="table" style="min-width:780px;">
        <thead>
          <tr>
            <th style="width:50%;">ペア1（編集可）</th>
            <th style="width:50%;">ペア2（編集可）</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <!-- ===== ペア1 ===== -->
            <td>
              <% ["pair1_member1", "pair1_member2"].each do |slot_key| %>
                <% mem = match.send(slot_key) %>
                <div class="slot-editor drop-target"
                     data-slot-el
                     data-drop-slot="<%= slot_key %>"
                     data-match-id="<%= match.id %>"
                     data-update-url="<%= update_slot_admin_event_path(match.round.event) %>">
                  <div class="info">
                    <div>
                      <strong data-current-name><%= mem&.name || "（未設定）" %></strong>
                      <% if mem %>（<%= mem.grade_label %>・<%= mem.gender_label %>・<%= mem.skill_label %>）<% end %>
                    </div>
                  </div>

                  <!-- 即時反映：スロットを空にする -->
                  <%= form_with url: update_slot_admin_event_path(match.round.event), method: :patch, local: true do %>
                    <%= hidden_field_tag :match_id, match.id %>
                    <%= hidden_field_tag :slot_key, slot_key %>
                    <%= hidden_field_tag :clear, "1" %>
                    <button class="btn btn-outline btn-compact" title="スロットを空にする">×</button>
                  <% end %>

                  <!-- 即時反映：セレクト差し替え（ボタン押下で反映） -->
                  <%= form_with url: update_slot_admin_event_path(match.round.event), method: :patch, local: true do %>
                    <%= hidden_field_tag :match_id, match.id %>
                    <%= hidden_field_tag :slot_key, slot_key %>
                    <%= select_tag :member_id,
                          options_from_collection_for_select(
                            @drag_members, :id,
                            ->(mm){ "#{mm.name}（#{mm.grade_label}・#{mm.gender_label}・#{mm.skill_label}）" }
                          ),
                          prompt: "選択して差し替え",
                          class: "select-compact",
                          data: {
                            member_select: true,
                            action: "change->lineup#bufferReplaceFromSelect", # 変更＝バッファに積む
                            match_id: match.id,
                            slot_key: slot_key
                          } %>
                    <button class="btn btn-primary btn-compact" title="選択で差し替え">🔁</button>
                  <% end %>

                  <!-- まとめて更新：バッファに積む（近くの select を参照） -->
                  <button class="btn btn-outline btn-compact"
                          data-action="click->lineup#bufferReplace"
                          data-match-id="<%= match.id %>"
                          data-slot-key="<%= slot_key %>"
                          data-select-ref="true">
                    ⏱ 差し替えをバッファ
                  </button>
                </div>
              <% end %>
            </td>

            <!-- ===== ペア2 ===== -->
            <td>
              <% ["pair2_member1", "pair2_member2"].each do |slot_key| %>
                <% mem = match.send(slot_key) %>
                <div class="slot-editor drop-target"
                     data-slot-el
                     data-drop-slot="<%= slot_key %>"
                     data-match-id="<%= match.id %>"
                     data-update-url="<%= update_slot_admin_event_path(match.round.event) %>">
                  <div class="info">
                    <div>
                      <strong data-current-name><%= mem&.name || "（未設定）" %></strong>
                      <% if mem %>（<%= mem.grade_label %>・<%= mem.gender_label %>・<%= mem.skill_label %>）<% end %>
                    </div>
                  </div>

                  <!-- 即時反映：スロットを空にする -->
                  <%= form_with url: update_slot_admin_event_path(match.round.event), method: :patch, local: true do %>
                    <%= hidden_field_tag :match_id, match.id %>
                    <%= hidden_field_tag :slot_key, slot_key %>
                    <%= hidden_field_tag :clear, "1" %>
                    <button class="btn btn-outline btn-compact" title="スロットを空にする">×</button>
                  <% end %>

                  <!-- 即時反映：セレクト差し替え（ボタン押下で反映） -->
                  <%= form_with url: update_slot_admin_event_path(match.round.event), method: :patch, local: true do %>
                    <%= hidden_field_tag :match_id, match.id %>
                    <%= hidden_field_tag :slot_key, slot_key %>
                    <%= select_tag :member_id,
                          options_from_collection_for_select(
                            @drag_members, :id,
                            ->(mm){ "#{mm.name}（#{mm.grade_label}・#{mm.gender_label}・#{mm.skill_label}）" }
                          ),
                          prompt: "選択して差し替え",
                          class: "select-compact",
                          data: {
                            member_select: true,
                            action: "change->lineup#bufferReplaceFromSelect",
                            match_id: match.id,
                            slot_key: slot_key
                          } %>
                    <button class="btn btn-primary btn-compact" title="選択で差し替え">🔁</button>
                  <% end %>

                  <!-- まとめて更新：バッファに積む -->
                  <button class="btn btn-outline btn-compact"
                          data-action="click->lineup#bufferReplace"
                          data-match-id="<%= match.id %>"
                          data-slot-key="<%= slot_key %>"
                          data-select-ref="true">
                    ⏱ 差し替えをバッファ
                  </button>
                </div>
              <% end %>
            </td>
          </tr>
        </tbody>
      </table>
    </div>
  </div>
<% end %>